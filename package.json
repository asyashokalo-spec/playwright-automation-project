{
  "name": "playwright-automation-project",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:ui": "playwright test --config=playwright.config.ts --project=Firefox",
    "test:api": "playwright test --config=playwright.api.config.ts --browser=firefox",
    "test:visual": "playwright test --config=playwright.visual.config.ts --project=Firefox"
  },
  "dependencies": {
    "@playwright/test": "^1.58.2"
  }
}